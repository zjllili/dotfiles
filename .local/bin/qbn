#!/usr/bin/sh
# @author nate zhou
# @since 2025

# fix crash on start with
# wlroots0.19, nvidia-open 575.64
/usr/bin/qutebrowser --qt-arg disable-gpu true \
    --basedir "${HOME}/.config/qutebrowser-n/" \
    --config-py "${HOME}/.config/qutebrowser/config.py.example"
