#!/usr/bin/sh
# @author nate zhou
# @since 2025
# update neomutt unread count for i3blocks

update() {
    pkill -SIGRTMIN+18 i3blocks
}

# update count on exit, interrupted or terminated
trap update EXIT INT TERM

command neomutt
