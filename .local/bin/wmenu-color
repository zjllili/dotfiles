#!/usr/bin/sh
# @author nate zhou
# @since 2025
# menu colorscheme, to be sourced by other scripts

. $HOME/.local/bin/colors.sh

wmenu -i -f "SourceCodePro medium 13" -S "$color9" -s "$background" -M "$color9" -m "$background" -N 111111 "$@"
