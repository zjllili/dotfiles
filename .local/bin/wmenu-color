#!/usr/bin/sh
# @author nate zhou
# @since 2025
# wmenu with colorscheme to be invoked by other scripts

. $HOME/.local/bin/colors.sh

wmenu -i -f "SourceCodePro medium 13" -S "$color6" -s "$background" -M "$color6" -m "$background" -N 111111 "$@"
