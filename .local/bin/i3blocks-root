#!/usr/bin/sh
# @author nate zhou
# @since 2025

usage="$(command df / | awk 'NR==2 {print $5}')"

icon="🗁"

case $BLOCK_BUTTON in
    2) footclient ncdu --disable-delete --exclude /home />/dev/null &
        ;;
esac

# ` `(U+2009) is the Thin Space
echo "$icon" "$usage"
