#!/usr/bin/sh
# @author nate zhou
# @since 2025


case $BLOCK_BUTTON in
    1) notify-send -u low -r 3301 "$icon $(basename $0)" "$(cal | sed '/^[[:space:]]*$/d')"
        ;;
    2) footclient -T "Floating Term" calcurse >/dev/null &
        ;;
esac


echo $(date '+%b-%d <small>%a</small> %H:%M')
