#!/usr/bin/sh
# @author nate zhou
# @since 2024,2025
# set live wallpapers, bind as xW in ranger
# @depends mpv, mpvpaper-run*

# in ranger, '@` and type 'setwall` or `:shell setwall %s` will set the current selection as the new wallpaper
cp -f "$1" "$HOME/.local/share/wallpaper"

# manually refresh by calling mpvpaper-run with sway keybindings
#$HOME/.local/bin/mpvpaper-run
